<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.ph.peach.dao.MemberDAO">
	<select id="selectMember" resultType="kr.ph.peach.vo.MemberVO">
		select * from member where
		me_id = #{me_id}
	</select>
	<insert id="insertMember">
		insert into member(me_id, me_pw, me_name, me_nick,
		me_phone, me_acc)
		values(#{member.me_id},#{member.me_pw},#{member.me_name},#{member.me_nick},#{member.me_phone},#{member.me_acc})
	</insert>

</mapper>
